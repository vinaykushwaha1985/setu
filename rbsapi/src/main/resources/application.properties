spring.main.allow-bean-definition-overriding=true

server.servlet.context-path=/rbsapi
server.servlet.session.timeout=43200000

server.max-http-header-size=65536


spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.SecurityAutoConfiguration
server.error.whitelabel.enabled=false

# Redis Config
spring.cache.type=redis
# REDIS (RedisProperties)
 # Redis database index (default is 0)
spring.redis.database=1  
 # Redis server address
spring.redis.host=100.65.156.203
 # Redis server connection port
spring.redis.port=6379  
 # Redis server connection password (default is empty)
spring.redis.password=  
 # Connection pool maximum number of connections (use negative values Ã¢â‚¬â€¹Ã¢â‚¬â€¹to indicate no limit)
spring.redis.jedis.pool.max-active=8  
 #Connection pool maximum blocking wait time (using a negative value means no limit)
spring.redis.jedis.pool.max-wait=-1
 # Maximum free connection in the connection pool
spring.redis.jedis.pool.max-idle=8  
 # The smallest idle connection in the connection pool
spring.redis.jedis.pool.min-idle=0  
 #Connection timeout (ms)
spring.redis.timeout=43200000




#Kafka Configuration
kafka.ben.card.status.update.url=http://localhost:8080/kmsapi/bis/update/card/status/2.0

kafka.ben.kyc.data.save.url=http://localhost:8080/kmsapi/bis/kyc/save/1.0
kafka.ben.demo.data.save.url=http://localhost:8080/kmsapi/bis/demo/save/1.0
kafka.ben.ration.data.save.url=http://localhost:8080/kmsapi/bis/ration/save/1.0
kafka.ben.secc.family.data.save.url=http://localhost:8080/kmsapi/bis/beneficiary/family/data/1.0

